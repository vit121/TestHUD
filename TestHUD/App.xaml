<Application x:Class="TestHUD.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="View/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="ColorBackgroundMain" Color="#1e1e1e"/>
            <Color x:Key="ColorDamageRed">Red</Color>
            <Color x:Key="ColorDamageOrange">Orange</Color>
            <Color x:Key="ColorDamageBlue">LightSkyBlue</Color>

            <BitmapImage x:Key="image_tankcompass_compass" UriSource="/TestHUD;component/Images/tankcompass_compass.png" />
            <BitmapImage x:Key="image_tankcompass_pad" UriSource="/TestHUD;component/Images/tankcompass_pad.png" />
            <BitmapImage x:Key="image_tankcompass_tower" UriSource="/TestHUD;component/Images/tankcompass_tower.png" />
            
            <BitmapImage x:Key="image_isEngineOverheat" UriSource="/TestHUD;component/Images/isEngineOverheat.png" />
            <BitmapImage x:Key="image_isOilLowPressure" UriSource="/TestHUD;component/Images/isOilLowPressure.png" />
            <BitmapImage x:Key="image_isEngineDamaged" UriSource="/TestHUD;component/Images/isEngineDamaged.png" />
            <BitmapImage x:Key="image_isHeadLightsOn" UriSource="/TestHUD;component/Images/isHeadLightsOn.png" />
            <BitmapImage x:Key="image_isAccumLowPower" UriSource="/TestHUD;component/Images/isAccumLowPower.png" />

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>

            <Style TargetType="{x:Type Image}">
                <Setter Property="RenderTransformOrigin" Value="0.5, 0.5"/>
            </Style>

            <!--Copy from MSDN-->
            <Style TargetType="{x:Type CheckBox}">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="#FF262E34"/>
                <Setter Property="Foreground" Value="HotPink"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="FontSize" Value="36" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" >
                                <Border BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" Width="15" Height="15">
                                    <Grid>
                                        <Grid Background="{TemplateBinding Foreground}" Margin="1" Visibility="Collapsed" Name="nullBlock"/>
                                        <Path Stretch="Uniform" Width="15" Height="10" Fill="{TemplateBinding Foreground}" Name="eliCheck" Data="F1 M 9.97498,1.22334L 4.6983,9.09834L 4.52164,9.09834L 0,5.19331L 1.27664,3.52165L 4.255,6.08833L 8.33331,1.52588e-005L 9.97498,1.22334 Z " Visibility="Collapsed"/>
                                    </Grid>
                                </Border>
                                <TextBlock Margin="5,0,0,0"  VerticalAlignment="Center" Foreground="White" Text="{TemplateBinding Content}"></TextBlock>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="LightGray" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF9C9E9F" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="LightGray" />
                                    <Setter Property="Foreground" Value="Gray" />
                                    <Setter Property="BorderBrush" Value="Gray"/>
                                    <Setter TargetName="eliCheck" Property="Opacity" Value="0.5" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="eliCheck" Property="Visibility" Value="Visible"></Setter>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="{x:Null}">
                                    <Setter TargetName="nullBlock" Property="Visibility" Value="Visible"></Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
